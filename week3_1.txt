function setup() { 
  createCanvas(400, 400);
  
  var ellipseH = 100;
  var ellipseW = 140;
  var ellipseX = 120;
  var ellipseY = 120;
  

} 
function draw()
{
	for(i = 0; i <= width; i = i+10)
	{
		line(200,0,i,400);
	}

}

